[tool.poetry]
name = "repl_python3_Own-Blueprint"
version = "0.1.0"
description = ""
authors = ["tutz95 <<>>"]

[tool.poetry.dependencies]
python = "^3.8"
Django = "^3.2.9"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
